function main() {
    var n = parseInt(readLine());
    arr = readLine().split(' ');
    arr = arr.map(Number);
    
    arrRev = arr.reverse();

    for (i = 0; i < n; i++) {
        process.stdout.write(arrRev[i] + ' ');
    };

};
